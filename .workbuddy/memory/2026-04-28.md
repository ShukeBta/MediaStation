# 2026-04-28 工作日志

## MediaStation NAS 家庭媒体服务器项目

### 前端完成
- **PlayerView.vue**: HLS.js 播放器，Direct Play + HLS 转码双模式，画质/倍速/音量/全屏/键盘快捷键/字幕/进度上报/自动下一集
- **DownloadView.vue**: 下载管理，客户端状态卡片、任务筛选/CRUD、SSE 实时状态
- **SubscribeView.vue**: 订阅管理，统计卡片、CRUD、质量/大小/关键词过滤、触发搜索
- **SitesView.vue**: 站点管理，多站搜索、一键下载、连接测试
- **SettingsView.vue**: 5选项卡（媒体库/下载客户端/通知渠道/定时任务/系统信息）
- **useSSE.ts**: SSE composable，指数退避自动重连

### 后端 Bug 修复
1. **database.py**: 清理冗余 Base 导入，统一使用 `base_models.Base`
2. **user/service.py**: `update_user` 中 `self.db` → 改用 `repo.update()`，并给 `UserRepository` 新增 `update()` 方法
3. **requirements.txt**: 添加 `psutil>=6.0`、锁定 `bcrypt>=4.0,<5.0`（passlib 兼容）
4. **subscribe/schemas.py**: `NotifyChannelOut` 补充 `config: dict` 字段，`_channel_to_out` 同步修复
5. **system/models.py**: `SettingsKV` 补充 `id`(PK)、`key`、`value` 列
6. **playback/service.py**: `get_play_info` 返回字段对齐前端（`stream_mode`/`direct_url`/`hls_playlist_url`/`episodes`/`watch_progress`），新增 `user_id` 参数
7. **media/repository.py**: 新增 `get_episodes_by_item()` 方法
8. **playback/router.py**: 传递 `user_id` 给 `get_play_info`

### 前端修复
1. **tsconfig.node.json**: 添加 `composite: true`，移除 `noEmit`
2. **MediaLibraryView.vue**: `@change="loadItems"` → `@change="() => loadItems()"` 修复 TS 类型
3. **SubscribeView.vue**: `getSubscriptions(params)` → `getSubscriptions(status)` 修复 TS 类型
4. **PlayerView.vue**: 字幕字段映射 `s.label` → `s.language_name`

### 构建验证
- **前端**: TypeScript 编译零错误，Vite build 成功（dist/ 约 780KB，各 view 独立 chunk）
- **后端**: FastAPI 启动成功，56 个路由注册，health check ✅，login API ✅（admin/admin123）

### README.md 编写
- 完成项目 README.md，包含：项目介绍/特性、系统架构图（ASCII）、技术栈、目录结构、Docker 部署 & 本地开发安装指南、完整环境变量配置表、API 概览（6 大模块）、功能模块说明、后台任务表、Nginx 反向代理配置、开发路线图（4 Phase）、前后端技术依赖清单

### 部署模板文件
- **docker-compose.example.yml**: Docker Compose 模板，主服务 + 可选 PostgreSQL + 可选 qBittorrent，支持 .env 环境变量注入，健康检查，硬件加速设备映射
- **nginx.example.conf**: Nginx 反向代理模板，HTTP→HTTPS 重定向、SSL、安全头、SSE 长连接、视频流 Range 支持
- README.md 更新：Docker 部署和 Nginx 部分改为引用模板文件，增加配置说明表

### Phase 5 UI/UX 升级
- **#41 搜索栏文字遮挡修复**: 搜索框从筛选面板内移到顶部标题栏旁独立区域，带搜索图标（左）+ 清除按钮（右），`pl-9` 左内边距避免文字图标重叠
- **#42 资源搜索独立区域**: 顶部新增全局搜索栏（max-w-md），placeholder 改为"搜索电影、电视剧、动漫..."，筛选面板不再包含搜索框
- **#43 新增发现页 DiscoverView.vue**: Banner 轮播（5s自动切换+手动箭头）、4个分类区块（最近电影/最近剧/高评分/动漫）、横滚卡片、渐变遮罩信息展示
- **#44 订阅管理标签页**: 新增「我的订阅」/「热门订阅」双 Tab 切换（pill 样式），热门订阅显示高评分媒体卡片+一键订阅功能
- **路由更新**: 新增 `/discover` 路由
- **侧边栏更新**: 新增「发现」导航项（搜索+链接组合图标），位于仪表盘和媒体库之间
